# @package _global_

sampler:
  name: video_once_sampler
  instance:
    _target_: airdc.common.samplers.video_sampler.VideoSamplerOnce
    # av_coder:
    #   non_monotonic_mode: drop
    #   non_monotonic_log: false

remove_mode: trash

dataset:
  directory: video_once
  file_extension: ".mp4"
